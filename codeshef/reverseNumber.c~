#include<stdio.h>
 
int main(){
 
 int T,n,reverse;
 scanf("%d",&T);
 while(T--){
   reverse = 0; 
   scanf("%d",&n);
   while(n > 0){
    reverse = reverse*10 + n%10;
    n = n/10;
   }
   printf("%d\n",reverse);
 }
 
return 0; 
}  
