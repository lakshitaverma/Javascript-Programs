#include<stdio.h>
 
int main(){
 
 int T,n,sum;
 scanf("%d",&T);
 while(T--){
   sum = 0; 
   scanf("%d",&n);
   while(n > 0){
    sum = sum + n%10;
    n = n/10;
   }
   printf("%d\n",sum);
 }
 
return 0; 
}  
